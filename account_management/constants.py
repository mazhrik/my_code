from django.utils.translation import gettext as _

Notification_Status = [
    ("fail", _('Get Failure status only')),
    ('success', _('Get Success status')),
    ('both', _('Both Failure and success'))
]
